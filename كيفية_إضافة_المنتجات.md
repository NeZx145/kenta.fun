# دليل إضافة المنتجات - How to Add Products Guide

## 📋 نظرة عامة / Overview

هذا الملف يشرح كيفية إضافة المنتجات إلى الموقع من خلال ملف `products.json`.

This file explains how to add products to the website through the `products.json` file.

---

## 📝 هيكل المنتج / Product Structure

كل منتج يجب أن يحتوي على الحقول التالية:

Each product must contain the following fields:

```json
{
  "id": 1,
  "name": "اسم المنتج",
  "category": "cheats",
  "subcategory": "external",
  "price": "$10",
  "rating": 0,
  "store": "اسم المتجر",
  "storeLink": "https://example.com",
  "image": "public/images/product-image.png"
}
```

---

## 🔧 شرح الحقول / Field Explanations

### 1. `id` (مطلوب / Required)
- **النوع**: رقم صحيح / Integer
- **الوصف**: رقم فريد لكل منتج / Unique number for each product
- **مثال**: `1`, `2`, `3`

### 2. `name` (مطلوب / Required)
- **النوع**: نص / String
- **الوصف**: اسم المنتج / Product name
- **مثال**: `"7mnyst1 Tweaker"`, `"Premium Spoofer"`

### 3. `category` (مطلوب / Required)
- **النوع**: نص / String
- **القيم المسموحة / Allowed values**:
  - `"cheats"` - الهاكات
  - `"spoofers"` - السبوفرات
  - `"tweaks"` - التويكات

### 4. `subcategory` (مطلوب / Required)
- **النوع**: نص / String
- **القيم المسموحة حسب القسم / Allowed values by category**:

  **للـ Cheats:**
  - `"external"` - خارجية
  - `"internal"` - داخلية

  **للـ Spoofers:**
  - `"permanent"` - دائمة
  - `"temporary"` - مؤقتة

  **للـ Tweaks:**
  - `"optimize"` - تحسين الحاسوب

### 5. `price` (اختياري / Optional)
- **النوع**: نص / String
- **الوصف**: سعر المنتج / Product price
- **مثال**: `"$10"`, `"$25.99"`, `"Free"`

### 6. `rating` (اختياري / Optional)
- **النوع**: رقم / Number
- **الوصف**: التقييم الافتراضي (0 يعني بدون تقييم) / Default rating (0 means no rating)
- **النطاق**: 0-10
- **مثال**: `0`, `8.5`, `10`

### 7. `store` (مطلوب / Required)
- **النوع**: نص / String
- **الوصف**: اسم المتجر / Store name
- **مثال**: `"7mnyst1"`, `"Premium Store"`

### 8. `storeLink` (مطلوب / Required)
- **النوع**: رابط / URL String
- **الوصف**: رابط المتجر / Store link
- **مثال**: `"https://example.com"`, `"https://store.com/product"`

### 9. `image` (مطلوب / Required)
- **النوع**: مسار الصورة / Image path
- **الوصف**: مسار صورة المنتج / Product image path
- **مثال**: `"public/images/product1.png"`
- **ملاحظة**: إذا لم توجد الصورة، سيتم استخدام الشعار الافتراضي / If image not found, default logo will be used

---

## 📄 مثال كامل / Complete Example

```json
[
  {
    "id": 1,
    "name": "7mnyst1 Tweaker",
    "category": "tweaks",
    "subcategory": "optimize",
    "price": "$10",
    "rating": 0,
    "store": "7mnyst1",
    "storeLink": "https://example.com",
    "image": "public/images/kenta-logo.png"
  },
  {
    "id": 2,
    "name": "Premium Spoofer",
    "category": "spoofers",
    "subcategory": "permanent",
    "price": "$25",
    "rating": 0,
    "store": "Premium Store",
    "storeLink": "https://example.com",
    "image": "public/images/kenta-logo.png"
  },
  {
    "id": 3,
    "name": "Game Cheat Pro",
    "category": "cheats",
    "subcategory": "external",
    "price": "$15",
    "rating": 0,
    "store": "Cheat Store",
    "storeLink": "https://example.com",
    "image": "public/images/kenta-logo.png"
  }
]
```

---

## 🎯 خطوات الإضافة / Adding Steps

### 1. افتح ملف `products.json`
Open the `products.json` file

### 2. أضف المنتج الجديد داخل المصفوفة `[]`
Add the new product inside the array `[]`

### 3. تأكد من:
Make sure:
- ✅ `id` فريد (لا يتكرر)
- ✅ `category` و `subcategory` صحيحة
- ✅ جميع الحقول المطلوبة موجودة
- ✅ الصيغة JSON صحيحة (فاصلة بعد كل عنصر ما عدا الأخير)

### 4. احفظ الملف
Save the file

### 5. حدّث الصفحة في المتصفح
Refresh the page in the browser

---

## ⚠️ ملاحظات مهمة / Important Notes

1. **تنسيق JSON**: تأكد من استخدام الفواصل بشكل صحيح
   - JSON Format: Make sure to use commas correctly
   - بعد كل عنصر ما عدا الأخير / After each element except the last one

2. **الأرقام**: لا تضع `id` أو `rating` بين علامات اقتباس
   - Numbers: Don't put `id` or `rating` in quotes
   - ✅ صحيح: `"id": 1`
   - ❌ خطأ: `"id": "1"`

3. **النصوص**: ضع جميع النصوص بين علامات اقتباس
   - Strings: Put all text in quotes
   - ✅ صحيح: `"name": "Product Name"`
   - ❌ خطأ: `"name": Product Name`

4. **الصور**: تأكد من وجود الصورة في المسار المحدد
   - Images: Make sure the image exists at the specified path
   - أو استخدم صورة افتراضية / Or use a default image

---

## 🔍 التحقق من الأخطاء / Error Checking

إذا لم يظهر المنتج:

If the product doesn't appear:

1. ✅ تحقق من صحة تنسيق JSON (استخدم JSON validator)
   - Check JSON format (use JSON validator)

2. ✅ تأكد من أن `id` فريد
   - Make sure `id` is unique

3. ✅ تأكد من أن `category` و `subcategory` صحيحة
   - Make sure `category` and `subcategory` are correct

4. ✅ افتح Console في المتصفح (F12) للتحقق من الأخطاء
   - Open Browser Console (F12) to check for errors

---

## 📚 أمثلة إضافية / Additional Examples

### مثال: منتج بدون سعر
Example: Product without price

```json
{
  "id": 10,
  "name": "Free Tool",
  "category": "tweaks",
  "subcategory": "optimize",
  "price": "",
  "rating": 0,
  "store": "Free Store",
  "storeLink": "https://example.com",
  "image": "public/images/free-tool.png"
}
```

### مثال: منتج مع تقييم
Example: Product with rating

```json
{
  "id": 11,
  "name": "Rated Product",
  "category": "cheats",
  "subcategory": "external",
  "price": "$20",
  "rating": 8.5,
  "store": "Rated Store",
  "storeLink": "https://example.com",
  "image": "public/images/rated-product.png"
}
```

---

## 🎨 نصائح / Tips

1. استخدم أسماء واضحة ووصفية للمنتجات
   - Use clear and descriptive product names

2. تأكد من جودة الصور (يفضل 800x600 أو أكبر)
   - Ensure image quality (preferably 800x600 or larger)

3. استخدم روابط صحيحة للمتاجر
   - Use valid store links

4. راجع المنتجات بعد الإضافة للتأكد من ظهورها بشكل صحيح
   - Review products after adding to ensure they appear correctly

---

## 📞 الدعم / Support

إذا واجهت أي مشاكل، تحقق من:
If you encounter any issues, check:

1. Console المتصفح للأخطاء / Browser Console for errors
2. تنسيق JSON / JSON format
3. مسارات الصور / Image paths
4. القيم المسموحة للحقول / Allowed field values

---

**آخر تحديث / Last Updated**: 2024

